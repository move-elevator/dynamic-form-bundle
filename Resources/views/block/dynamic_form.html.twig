{%- block dynamic_form -%}
    {% for element in form.vars.elements -%}
        {% if element is FormElement %}
            {{ form_element(element) }}
        {% else %}
            {{ form_row(form.children[element.name]) }}
        {% endif %}
    {%- endfor %}
{% endblock %}